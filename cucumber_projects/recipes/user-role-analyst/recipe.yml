name: Cucumber projects - analyst 
description: A recipe to manage needed change of configs for the analyst user role.
type: Testing
config:
  actions:
    user.role.analyst:
      grantPermissions:
        - 'access projects page'
