name: Cucumber projects - product owner 
description: A recipe to manage needed change of configs for the product owner user role.
type: Testing
config:
  actions:
    user.role.product_owner:
      grantPermissions:
        - 'access projects page'
